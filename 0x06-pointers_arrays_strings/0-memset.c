#include <stdio.h>

/**
 * _memset - function to fill memory
 * @s: Pointer which holds the value
 * @b: the constant
 * @n: variable that will be filled with the constant
 * Return: pointer *s
 */
char *_memset(char *s, char b, unsigned int n)
{
	int j;
}
