#include "main.h"

/**
 * print_diagsums - give the sum of the diagonals
 * @a: pointer storing the arrays
 * @size: size of the arrays
 * return: void
 */
int add(int x, int y)
{
	return x + y;
}


void print_diagsums(int *a, int size)
{
	int m, n, sum1, sum2;

	sum1 = 0;
	sum2 = 0;

	for (n = 0; n < size; n++)
	{
		for (m = 0; m < size; m++)
		{
			if (i == j)
			{
				sum1 = add(sum1, a[m * size + j]);
			}
			if (i + j == size - 1)
	
		}
	}
	_putchar(a[m][n]);
	_putchar('\n');

}
